import { HumanViewPipe } from './human-view.pipe';

describe('HumanViewPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanViewPipe();
    expect(pipe).toBeTruthy();
  });
});
